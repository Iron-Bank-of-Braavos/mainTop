// var server = require('../../server/index.js')

afterAll(function () {
  // globalDatabase.cleanUp();
  // server.close();
});

test('should test if server is listerning the port', () => {
    // var port = server._connectionKey.slice(5);
    // expect(port).toEqual('3000');
    // setTimeout( function() { server.close(); done() } ) 
}) ;